name = "chat"
version = "0.1.0"
description = "Standalone chat example resource"
authors = ["MeowV Team"]
license = "MIT"
tags = ["chat", "example"]
protocol_version = 2
edition_compatibility = "any"
platform_compatibility = "any"

[entrypoints]
server = "server/main.js"
client = "client/main.js"
